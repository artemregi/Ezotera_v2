/**
 * Palmistry Reading Mixer — Exponential Variation System
 *
 * Дополнительная система для еще большего разнообразия.
 * Вместо выбора одного полного текста из 30, можно разбить текст
 * на компоненты (введение, ключевой инсайт, завершение) и комбинировать их.
 *
 * Это даёт:
 * - 3 варианта введения × 3 основных инсайта × 3 завершения = 27 комбинаций
 * - Умножить на 30 вариаций основной темы = 810+ различных способов
 */

'use strict';

// ============================================================================
// КОМПОНЕНТЫ ДЛЯ ДИНАМИЧЕСКОЙ КОМБИНАЦИИ
// ============================================================================

// Введение в любовный раздел
const LOVE_INTRO = [
  'Линия сердца говорит о вашей способности на глубокую, подлинную любовь.',
  'В вашей ладони запечатлена редкая способность: любить полностью, без половинок.',
  'Ваше сердце — это ваша сила, и ладонь это подтверждает.',
];

// Основные инсайты о любви (могут следовать за основным текстом)
const LOVE_INSIGHTS = [
  'Если прошлое причиняло боль — это учителем, не врагом. Впереди период исцеления.',
  'Вам нужна не просто любовь, а партнёрство с равным. Ладонь показывает: вы найдёте.',
  'Ваша главная задача — позволить себе быть любимым так же, как вы любите сами.',
];

// Завершение блока о любви
const LOVE_CLOSING = [
  'Припарируйтесь к встрече, которая изменит всё.',
  'Ваше сердце уже знает, что впереди. Разум скоро поймёт.',
  'Это не вопрос "будет ли" — это вопрос "когда".',
];

// ============================================================================

// Введение в карьеру
const CAREER_INTRO = [
  'Линия головы и линия судьбы образуют редкое сочетание в вашей ладони.',
  'Ваш потенциал давно превышает текущие границы.',
  'Ладонь указывает: вы готовы к следующему уровню.',
];

// Основные инсайты о карьере
const CAREER_INSIGHTS = [
  'Ваша природная способность видеть суть станет конкурентным преимуществом.',
  'Не финансы — смысл вашей работы. И это приведёт вас в правильное место.',
  'Лидерство для вас не громкое — оно тихое и реальное. Готовьтесь.',
];

// Завершение блока о карьере
const CAREER_CLOSING = [
  'Возможность появится скоро. Не пропустите.',
  'Ваши решения в следующие 6 месяцев будут ключевыми.',
  'То, что вы создадите, будет значимо не только для вас.',
];

// ============================================================================

// Введение в судьбу
const DESTINY_INTRO = [
  'Общий рисунок вашей ладони расскажет историю трансформации.',
  'Ваша судьба — это не скучная линия, это цикл с глубоким смыслом.',
  'Ладонь показывает: вы находитесь в точке перемен.',
];

// Основные инсайты о судьбе
const DESTINY_INSIGHTS = [
  'Каждая прошлая боль была учителем. Она подготовила вас к большему.',
  'Вы сильнее, чем вы верите. Впереди — ясность в этом.',
  'Влияние, которое вы оказываете, шире, чем вы осознаёте.',
];

// Завершение блока о судьбе
const DESTINY_CLOSING = [
  'История, которую вы пишете, вдохновляет других, даже если вы это не видите.',
  'Главная встреча ещё впереди. Готовьтесь к неожиданности.',
  'Вы на пороге чего-то большого. Почувствуйте это.',
];

// ============================================================================
// UTILITY: Собрать кастомный текст из компонентов
// ============================================================================

function pick(arr, rng) {
  return arr[Math.floor(rng() * arr.length)];
}

/**
 * Собрать динамический текст о любви из компонентов
 */
function generateCustomLoveText(rng) {
  const intro    = pick(LOVE_INTRO, rng);
  const insight  = pick(LOVE_INSIGHTS, rng);
  const closing  = pick(LOVE_CLOSING, rng);

  return `${intro} ${insight} ${closing}`;
}

/**
 * Собрать динамический текст о карьере из компонентов
 */
function generateCustomCareerText(rng) {
  const intro    = pick(CAREER_INTRO, rng);
  const insight  = pick(CAREER_INSIGHTS, rng);
  const closing  = pick(CAREER_CLOSING, rng);

  return `${intro} ${insight} ${closing}`;
}

/**
 * Собрать динамический текст о судьбе из компонентов
 */
function generateCustomDestinyText(rng) {
  const intro    = pick(DESTINY_INTRO, rng);
  const insight  = pick(DESTINY_INSIGHTS, rng);
  const closing  = pick(DESTINY_CLOSING, rng);

  return `${intro} ${insight} ${closing}`;
}

// ============================================================================
// Экспорт
// ============================================================================

module.exports = {
  LOVE_INTRO, LOVE_INSIGHTS, LOVE_CLOSING,
  CAREER_INTRO, CAREER_INSIGHTS, CAREER_CLOSING,
  DESTINY_INTRO, DESTINY_INSIGHTS, DESTINY_CLOSING,
  generateCustomLoveText,
  generateCustomCareerText,
  generateCustomDestinyText,
};
