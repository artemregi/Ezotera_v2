# Database Connection (from Vercel Postgres or Supabase)
# Format: postgresql://username:password@host:port/database
POSTGRES_URL=postgresql://user:pass@localhost:5432/ezoterra

# JWT Secret (generate with: openssl rand -base64 32)
JWT_SECRET=your-super-secret-jwt-key-here-256-bits

# Application Environment
NODE_ENV=production
APP_URL=https://ezoterra.vercel.app

# Optional: For rate limiting with Upstash Redis (production enhancement)
# UPSTASH_REDIS_URL=
# UPSTASH_REDIS_TOKEN=
